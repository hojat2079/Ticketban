import 'package:flutter/material.dart';

class AnsweredTicketItem extends StatelessWidget {
  const AnsweredTicketItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
